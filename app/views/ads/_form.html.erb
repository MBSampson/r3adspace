<%= form_with(model: ad, local: true) do |form| %>
  <% if ad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ad.errors.count, "error") %> prohibited this ad from being saved:</h2>

      <ul>
      <% ad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-3">
      <div class="form-group">
        <%= form.label :title %>
        <%= form.text_field :title, id: :ad_title, class: "form-text" %>
      </div>

      <div class="form-group">
        <%= form.label :price %>
        <%= form.text_field :price, id: :ad_price, class: "form-text" %>
      </div>
    </div>

    <div class="col-md-6">
      <div class="form-group">
        <%= form.label :description %>
        <%= form.text_area :description, id: :ad_description, class: "form-text" %>
      </div>
    </div>

    <div class="col-md-3">
      <div class="form-group">
        <%= form.label :image %>
        <%= form.text_field :image, id: :ad_image, class: "form-text" %>
      </div>
    </div>
  </div>

    <div class="row">
      <div class="col-md-9">

      </div>
      <!-- <div class="col-md-3">
        <div class="action">
          <input class="btn btn-warning" data-disable-with="Update Ad" name="commit" type="submit" value="Update Ad">
          <%= link_to 'Cancel', @ad, class: "btn btn-primary" %>
        </div>
      </div> -->
      <div class="col-md-3">
        <!-- <input class="btn btn-success" data-disable-with="Create Ad" name="commit" type="submit" value="Create Ad"> -->
        <p><%= submit_form_helper(@form_type) %></p>
      </div>
    </div>
<% end %>
